# 竖屏支持修复进度（2026-07-26 晚）

## 已完成

### 1. 合成星核 H5（app 889164）竖屏修复 + 已重新提交审核
- **修复内容**：
  - `taptap-game/css/style.css`：小屏竖屏下菜单/面板被截断的问题已修复（面板改为可滚动、最大高度限制、移动端适配）。
  - `manifest.json`：`orientation` 由 `any` 改为 `portrait`，描述同步改为竖屏休闲玩法。
  - 同步到 Android assets：`css/style.css`、`js/game.js`、`manifest.json`。
- **构建**：`starcore-merge-h5-taptap-v1.2.2.zip`（版本号 128609，158.73 KB）。
- **平台操作**：上传 v1.2.2 → 创建版本 → 补齐缺失游戏图标 → 提交审核。
- **当前状态**：**审核中**（版本 V-20260721）。
- **注意**：平台包体卡片仍显示「屏幕方向：横屏」，疑似应用创建时锁定的平台元数据；游戏内已做竖屏布局适配。

### 2. 星核收集（app 889543）反弹板控制 bug 修复 + 已发布
- 修复底部收集器松手后被重力感应拉向左侧卡住的 bug。
- 体感默认关闭，新增「体感/触屏」切换按钮。
- Maker 构建已成功推送（commit `c81408a`）。

## 配置已确认 / 竖屏代码无误

| 游戏 | appid | 竖屏配置 | 状态 |
|------|-------|----------|------|
| 合成星核 H5 | 889164 | CSS 已适配 + manifest portrait | 审核中 |
| 合成星核小游戏 | 893210 | `game.json` `deviceOrientation: "portrait"` | 未发布，缺素材 |
| 贪吃蚯蚓 | 889330 | `AndroidManifest.xml` `screenOrientation="portrait"` | 审核失败，需换截图 |

## 仍需处理（阻塞项）

### A. 贪吃蚯蚓 889330 — 审核失败
- **反馈**：
  1. 游戏截图方向/尺寸不一致；
  2. 存在重复图片；
  3. 一张截图含 AI 生成内容。
- **需要**：为该原生 Android 游戏重新准备一组统一尺寸/方向的竖屏真实截图，上传后重新提交。

### B. 合成星核小游戏 893210 — 未发布
- **缺 3 项**：游戏素材（宣传图 + 游戏截图）。
- **另有**：前置备案/电子化核验需运营负责人用微信/支付宝扫码完成人脸识别。
- **需要**：补齐素材 + 完成人脸识别后提交审核。

## 建议
- 如要继续推进，下一步优先处理 **889330 截图** 和 **893210 素材**。
- 889164 当前已提交，等审核结果；若审核因宣传图/交流群链接风险提示被驳回，再统一修图。
