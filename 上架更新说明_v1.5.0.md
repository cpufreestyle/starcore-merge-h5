# 合成星核 · 版本更新说明 v1.5.0

> 上一版：**v1.4.1**（已含真实广告 `js/ads.js`、支付服务 `server/`、WebView 画布修复）
> 本次版本号建议：**v1.5.0**

---

## 一句话亮点

**进度不再丢**——随时退出、随时回来接着玩；新增离线收益、连击可视化、生涯统计与新手引导，上手更快，成长看得见。

---

## 更新内容（玩家视角）

### 💾 对局存档续玩
- 中途刷新、误关、切后台回来，**进度不再丢失**。
- 开局后菜单顶部自动出现「▶️ 继续上局（模式 + 分数）」，一键回到上次局面。
- 完整保留：棋盘核心、分数、能量条、道具存量、解谜关卡与剩余步数、计时模式已用时间。

### 🌙 离线收益
- 离开期间星核持续产出，回来自动结算并发放星核币。
- 速率 6 💎/小时，累计上限 12 小时（72 💎），不足 1 分钟不结算。

### 🔗 连击计时条
- 原本「隐形」的 1.5 秒连击窗口现在可视化为倒计时条，能直观判断连击还能不能接上。

### 🎮 新手引导
- 首次进入展示玩法说明（合成升级 / 7 级爆发 / 彩虹核心 / 连击加成 / 四种道具），之后不再打扰。

### 📊 生涯统计
- 新增跨局累计数据面板：总局数、累计得分、历史最高、**场均得分**、累计合成、累计爆发、最高连击、彩虹核心、连击风暴、星核觉醒。

### ⚡ 性能与稳定性
- **存档写入节流**：localStorage 是同步 IO，此前每次合并/用道具都全量序列化落盘，连击时有可感知卡顿；现 500ms 合并写入，页面隐藏/关闭时立即落盘，进度不丢。
- 点击判定由遍历 25 格改为数学直接定位（O(1)）。
- HUD 增加脏检查，值未变化不写 DOM。
- 星云改为离屏缓存、核心高光色预计算、后台标签页暂停渲染，更省电流畅。
- 广告 SDK 域名加 `dns-prefetch` / `preconnect`，首次看广告更快。

### 📦 PWA 离线支持
- 支持「安装到桌面」与离线打开（需 https 环境）。

---

## ⚠️ 上架前必须完成的两件事

### 1. 填入真实广告位 ID（否则仍走模拟广告）
`index.html` 中已预留配置入口，当前为空，游戏会自动降级到**模拟广告**（仅调试用，不建议随正式包上架）：

```html
<script>
  window.AD_CONFIG = {
    ylhAppId: '',   // 优量汇应用 ID
    ylhSlotId: '',  // 优量汇激励视频广告位 ID
  };
</script>
```

需在优量汇后台创建「激励视频」广告位并绑定投放域名、完成域名校验，否则广告不填充（`localhost` 不会产生真实填充）。

### 2. 重新构建 APK
H5 已同步进 `android/app/src/main/assets/web/`，但**必须重新打包**，APK 才会包含本次更新（此前壳内仍是旧版 H5，甚至缺少 `ads.js`）。

---

## 提交前自检清单

- [ ] 开局后刷新页面，「继续上局」按钮出现且能正确恢复棋盘与分数
- [ ] 关闭页面等待 >1 分钟再打开，弹出离线收益提示
- [ ] 连续合成时连击计时条出现并随时间收缩，中断后消失
- [ ] 首次进入（清 localStorage）弹出新手引导，点击后不再出现
- [ ] 菜单「📊 生涯统计」能打开且数字随对局累加
- [ ] 广告入口（📺）流程完整；**已填入真实广告位 ID**（若上架正式包）
- [ ] 支付链路（原生壳内微信/支付宝）可用
- [ ] `android/app/src/main/assets/web/` 内 index.html 引用了 `js/ads.js`
- [ ] 后台版本号填写为 **v1.5.0**

---

## 分支说明

本地功能开发分支为 `local/features`（含压缩构建等早期方案）。当前 `main` 以远程为基线，已合并其中的游戏功能；早期方案中与远程重复的部分（`js/admgr.js` 广告适配器、terser 压缩产物）**未采用**，统一使用远程的 `js/ads.js` 与源码引用方式。

---

## Android 包体同步状态

已同步至 `android/app/src/main/assets/web/`：

| 文件 | 说明 |
|---|---|
| `index.html` | 含连击条、引导、生涯统计 DOM，引用 `js/ads.js` |
| `css/style.css` | 含连击条、生涯统计、引导样式 |
| `js/audio.js` `ads.js` `shop.js` `game.js` | 源码（与远程一致的引用方式） |
| `sw.js` | PWA（WebView 用 file:// 时不会注册，无害） |

---

## APK 构建与上传（需你本人操作）

构建依赖以下**仅在你手里的凭证**：

1. **广告 SDK 包**：`dirichlet_ad_4.2.8.0.aar` 放入 `android/app/src/main/libs/`
2. **支付 AppID**：`MainActivity.java` 中填 `WX_APP_ID` / `ALIPAY_APP_ID`
3. **构建环境**：Android SDK 34 + JDK 11+ + Gradle
4. **签名 keystore**：release 签名凭证

```bash
cd android
./gradlew assembleRelease   # 输出 app/build/outputs/apk/release/app-release.apk
```

上传：开发者中心 → 版本信息 → 创建新版本 → 上传 APK → 版本号填 **v1.5.0** → 更新说明用本文「更新内容」。

> 若希望用浏览器自动化代填上传，需提供：已登录 developer.taptap.cn 的 Chrome（远程调试模式 `--remote-debugging-port=49351`）+ APK 本地路径。
