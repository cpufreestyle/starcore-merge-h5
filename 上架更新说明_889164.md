# 合成星核 · 版本更新说明（TapTap 后台 889164）

> 适用页面：`https://developer.taptap.cn/v3/428430/app/889164/store/update`
> 本次版本号建议：**v1.2.0**

---

## 一句话亮点

全新「横竖屏自由切换」+ 内置「激励广告」与「星核币商店」，手机、平板都能爽玩。

---

## 更新内容

### 🔄 横竖屏自适应切换（本次重点）
- 新增右上角 **↻ 旋转按钮**，一键在 `自动 → 竖屏 → 横屏` 之间循环。
- 横屏模式下自动切换为 **左右分栏布局**：棋盘占主区域，能量条 / 工具栏移到右侧，信息更清晰。
- 适配手机与平板，竖握、横握都不再有黑边或遮挡。
- 浏览器不支持自动锁屏时，会提示「请旋转你的设备以切换横竖屏」，手动旋转同样生效。

### 📺 激励视频广告
- 关键时机可**看广告续命 / 领取星核币**，不卡流程、不打断体验。
- 广告为可选激励形式，看完才发奖，绝不强制。

### 🛒 星核币商店（内购）
- 提供多档商品：**星核币礼包（100 / 600）**、**永久去广告**、**VIP 月卡**。
- 支付走原生桥接（Android 壳内微信 / 支付宝），H5 环境自动 fallback 模拟支付，体验一致。

### 🐞 体验优化与修复
- 修正部分机型布局错位、工具栏被遮挡的问题。
- 提升横竖屏切换时的画面重排稳定性。
- manifest 与工程配置对齐（方向 `any`、版本号同步至 1.2.0）。

---

## 提交前自检清单

- [ ] 已用本地预览（http://127.0.0.1:8080）确认横屏 / 竖屏布局正常
- [ ] 广告入口（📺）点击可正常弹出激励视频
- [ ] 商店入口（🛒）点击可正常打开并购买
- [ ] 旋转按钮在竖屏 / 横屏下均能切换
- [ ] 包体已包含 `index.html` + `css/` + `js/`（game.js / shop.js）
- [ ] TapTap 后台版本号填写为 **v1.2.0**

---

## 商店文案参考（可直接贴商品描述）

- **星核币 ×100**：新手起步礼包，畅玩更轻松。
- **星核币 ×600**：超值囤币，长期玩家首选。
- **永久去广告**：一次购买，全程清爽无干扰。
- **VIP 月卡**：每月专属特权，畅享合成乐趣。

---

## ⚠️ 游戏包（APK）构建与上传 —— 这是「更新到最新版本」的核心，目前无法由 AI 端到端代做

889164 是包了 H5 的 **Android App**，TapTap 上架的是 **release 签名 APK**（H5 在构建时打进 `android/app/src/main/assets/web/`）。AI 助手无法替你完成，原因是三个缺口都涉及**你的账号/密钥**：

### 构建 APK 前必须补齐的依赖（均在你本人手里）
1. **广告 SDK 包**：从你的 Dirichlet 后台下载 `dirichlet_ad_4.2.8.0.aar`，放进 `android/app/src/main/libs/`。
2. **支付 AppID**：在 `android/app/src/main/java/com/starcore/merge/MainActivity.java` 填入
   - `WX_APP_ID`（微信开放平台，需绑定包名 `com.starcore.merge` + 应用签名）
   - `ALIPAY_APP_ID`（支付宝开放平台）
3. **构建环境**：Android SDK 34 + JDK 11+ + Gradle（本机当前 `ANDROID_HOME` 为空、无 Gradle，需安装）。
4. **签名 keystore**：release 签名必需，属于你的发布凭证（AI 没有、也不应经手）。

> ✅ 已帮你做的一步（无需密钥）：把本次最新的 H5（横竖屏+广告+商店）**已同步进 `android/app/src/main/assets/web/`**（index.html / css/style.css / js/game.js|shop.js|audio.js / manifest.json）。构建时壳会自动加载最新版，不会包进旧 H5。

### 构建 APK（二选一）
- **Android Studio**：Open `taptap-game/android/` → 填好上面的 ID → Build → Generate Signed Bundle/APK → Build APK。
- **Gradle 命令行**：
  ```bash
  cd taptap-game/android
  ./gradlew assembleRelease        # 输出 app/build/outputs/apk/release/app-release.apk
  ```
  （仓库当前无 `gradlew`、无 Gradle，需先 `sdkmanager` 装 SDK 34 + 用你自己的 Gradle/Android Studio。）

### 上传到 TapTap
1. 登录 `https://developer.taptap.cn/v3/428430/app/889164/store/update`。
2. **版本信息**页 → 创建新版本 → 上传刚打好的 `app-release.apk`（APK 会替代当前 H5 包体）。
3. 版本号填 **v1.2.0**，更新说明用本文上方「更新内容」。
4. 保存提交，等待审核。

### 如果你希望我后续用浏览器自动化代点上传
需要你提供两样东西，我才能驱动「版本信息」页：
- (a) 一个**已登录 developer.taptap.cn** 的 Chrome，并以远程调试模式打开（`--remote-debugging-port=49351`）；
- (b) 构建好的 APK 本地路径。
届时可连接 CDP 自动填版本号 + 更新说明 + 选包体 + 点保存。宣传物料（16:9/1:1 图 + 宣传片）的自动上传同理，也需要这个登录态。
